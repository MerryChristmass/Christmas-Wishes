{
  "name": "ar-christmas-song-pic",
  "version": "1.0.0",
  "description": "Christmas Tree with song",
  "main": "index.html",
  "scripts": {
    "deploy": "gh-pages -d build",
    "predeploy": "yarn run build",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mona",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
